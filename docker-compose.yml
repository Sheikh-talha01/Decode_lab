version: '3.8'

services:
  app:
    build: .
    image: decode_lab:local
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    ports:
      - "8000:8000"
    command: uvicorn src.decode_lab.api:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
