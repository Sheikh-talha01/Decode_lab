name: CI

on:
  push:
    branches: [ main, feature/project1 ]
    tags: ['v*']
  pull_request:

jobs:
  project1-tests:
    name: Project1 tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r project1/requirements.txt
      - name: Run project1 tests
        run: pytest -q project1/tests

  project2-tests:
    name: Project2 tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r project2/requirements.txt
      - name: Run project2 tests
        run: |
          export PYTHONPATH=project2/src || set PYTHONPATH=project2/src
          pytest -q project2/tests

  build-and-publish:
    name: Build and publish Docker images (optional)
    runs-on: ubuntu-latest
    needs: [project1-tests, project2-tests]
    # This job publishes to DockerHub when DOCKERHUB secrets exist,
    # and will also publish to GHCR on tag pushes via the publish-ghcr job below.
    if: ${{ secrets.DOCKERHUB_USERNAME && secrets.DOCKERHUB_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      - name: Build and push project1 image
        uses: docker/build-push-action@v5
        with:
          context: project1
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/decode_lab:project1-${{ github.sha }}
      - name: Build and push project2 image
        uses: docker/build-push-action@v5
        with:
          context: project2
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/decode_lab:project2-${{ github.sha }}

  publish-ghcr:
    name: Publish images to GHCR on tags
    runs-on: ubuntu-latest
    needs: [project1-tests, project2-tests]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push project1 image to GHCR
        uses: docker/build-push-action@v5
        with:
          context: project1
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/decode_lab-project1:${{ github.ref_name }}
      - name: Build and push project2 image to GHCR
        uses: docker/build-push-action@v5
        with:
          context: project2
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/decode_lab-project2:${{ github.ref_name }}
